# SkillVault backend — direct dependencies
#
# This lists only the packages actually imported by the code (pip will
# resolve their transitive dependencies automatically). Grouped by what
# each one is for, so it's obvious what to remove if a feature goes away.
#
# Install with:
#   pip install -r requirements.txt
#
# System-level (non-pip) requirements:
#   - Postgres server (DATABASE_URL) — psycopg2-binary below is just the driver
#   - poppler-utils (apt install poppler-utils) — required by pdf2image
#   - tesseract-ocr (apt install tesseract-ocr) — required by pytesseract

# --- Web framework / server ---
fastapi==0.141.1
uvicorn==0.52.1
starlette==1.6.0
python-multipart==0.0.32          # needed for FastAPI File()/Form() (video/manual/audio uploads)
pydantic==2.13.4

# --- Database ---
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12           # Postgres driver

# --- Auth / config ---
bcrypt==5.0.0                     # password hashing
python-dotenv==1.2.2              # loads .env (config.py)

# --- Rate limiting ---
slowapi==0.1.10

# --- Vector store (RAG) ---
chromadb==1.5.9

# --- LLM / embeddings (Gemini) ---
google-genai==2.17.0

# --- PDF ingestion (manuals) ---
pypdf==6.15.0
pdf2image==1.17.0                 # requires poppler-utils on the system
pytesseract==0.3.13                # requires tesseract-ocr on the system

# --- HTTP client (Sarvam TTS/STT, misc calls) ---
requests==2.34.2