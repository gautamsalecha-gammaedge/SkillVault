# from db import engine
# from sqlalchemy import text

# with engine.connect() as conn:
#     conn.execute(text("ALTER TABLE workers ADD COLUMN is_approved BOOLEAN NOT NULL DEFAULT FALSE"))
#     conn.commit()